# ❓ FAQ — คำถามที่พบบ่อย (ระบบยืมคืนหนังสือ)

> เอกสารนี้รวบรวมคำถามที่ลูกค้าถามบ่อยหลังซื้อ Template
> อ่านจบแล้วจะเข้าใจว่า "ได้อะไร / ใช้ยังไง / ทำอะไรได้-ไม่ได้"

---

## 📦 1. คำถามเกี่ยวกับสินค้า

### Q: Template นี้คืออะไร?

**A:** เป็นชุดโค้ด PHP สำเร็จรูปของ **"ระบบยืมคืนหนังสือ"** ที่ทำงานได้จริง ประกอบด้วย:
- ระบบ login / สมัครสมาชิก
- จัดการหนังสือ (เพิ่ม, แก้, ลบ, import CSV)
- ยืม-คืน พร้อมคำนวณค่าปรับอัตโนมัติ
- จองหนังสือ
- รายงานสถิติ + Export PDF/CSV
- พิมพ์บัตรสมาชิก
- ระบบ admin + staff + member แยก role

ไม่ต้องเขียนโค้ดเองตั้งแต่ศูนย์ แค่ติดตั้งแล้วใช้ได้เลย

---

### Q: เหมาะกับใคร?

**A:**
- ✅ **นักเรียน / นักศึกษา** ที่ต้องส่ง project จบ
- ✅ **คนที่อยากเรียนรู้ PHP + Backend** จากตัวอย่างจริง
- ✅ **คนที่อยากเดโมระบบ** ให้ลูกค้าหรืออาจารย์ดู
- ✅ **ห้องสมุดเล็กๆ / ร้านเช่าหนังสือ** ที่มีผู้ใช้ไม่มาก
- ✅ **ผู้ที่ต้องการ template** เพื่อนำไปปรับแต่งเป็นโปรเจคของตัวเอง

---

### Q: ไม่เหมาะกับใคร?

**A:**
- ❌ คนที่ต้องการระบบ **ใช้งานจริงระดับองค์กรใหญ่** (หลายร้อย-พันคนพร้อมกัน)
- ❌ คนที่ต้องการระบบที่ **online 24/7 ไม่มีวันล่ม**
- ❌ คนที่ต้องการระบบที่ **ผ่าน security audit ระดับสูง**
- ❌ คนที่ต้องการ **ระบบหลายภาษา** หรือ **mobile app**
- ❌ คนที่คาดหวังว่าจะได้ **software สำเร็จรูประดับ enterprise** ในราคา 499 บาท

---

### Q: ใช้เรียนได้ไหม?

**A:** ได้เลย! นี่เป็นจุดแข็งของ template นี้ โค้ดมี comment อธิบายทุกจุดสำคัญ + มีเอกสารประกอบหลายไฟล์ ครอบคลุม:
- Layered Architecture (Controller → Service → Repository)
- Transaction + Concurrency Control
- Authentication + Security
- CRUD + Validation
- State Management (ระบบจอง)
- Report + Export

---

### Q: ใช้ส่งงาน / ส่ง project จบได้ไหม?

**A:** ได้ แต่แนะนำให้ **อ่านทำความเข้าใจโค้ดก่อน** เผื่ออาจารย์ถามว่า "ส่วนนี้ทำงานยังไง?" จะได้ตอบได้ เอกสาร `FLOW.md` และ `README.md` จะช่วยให้เข้าใจภาพรวมได้เร็วขึ้น

---

## 🧠 2. คำถามสำหรับมือใหม่

### Q: ไม่มีพื้นฐานเขียนโค้ดเลย ใช้ได้ไหม?

**A:** ใช้ **ติดตั้งและทดลองได้** เพราะมีคู่มือติดตั้งอธิบายทุกขั้นตอน

แต่ถ้าจะ **แก้ไขโค้ด** หรือ **เพิ่มฟีเจอร์** ควรมีพื้นฐาน PHP + MySQL บ้าง หรือเรียนรู้ไปพร้อมกัน โค้ดชุดนี้มี comment ภาษาไทยช่วยอธิบาย จะช่วยให้เรียนรู้ได้เร็วขึ้น

---

### Q: ต้องอ่านไฟล์ไหนก่อน?

**A:** อ่านตามลำดับนี้:

1. **`README.md`** — ภาพรวมทั้งหมด, ติดตั้งยังไง, ใช้ยังไง
2. **`FLOW.md`** — ระบบทำงานยังไง อ่านแล้วเห็นภาพ
3. **`SUPPORT.md`** — ถามอะไรได้ / อะไรไม่ได้

ถ้าอยากอ่านโค้ดจริง:
4. **`bootstrap.php`** → เข้าใจว่าระบบเริ่มต้นยังไง
5. **`login.php`** → ตัวอย่าง Controller แรกที่ควรอ่าน
6. **`app/Services/AuthService.php`** → ตัวอย่าง Service แรกที่ควรอ่าน

---

### Q: โค้ดเยอะไหม? จะงงไหม?

**A:** โค้ดมีหลายไฟล์จริง เพราะ **ออกแบบให้แยกหน้าที่ชัดเจน** ไม่ใช่ยัดทุกอย่างไว้ไฟล์เดียว

แต่ข้อดีคือ: ไม่ต้องอ่านทุกไฟล์ ให้เลือกอ่าน **ตาม flow ที่สนใจ** เช่น:
- อยากรู้ "login ทำยังไง" → อ่านแค่ `login.php` + `AuthService.php`
- อยากรู้ "ยืมหนังสือทำยังไง" → อ่านแค่ `admin/borrow_form.php` + `BorrowService.php`

ดูรายละเอียดเพิ่มเติมได้ที่ `FLOW.md` หัวข้อ "วิธีอ่านโค้ด"

---

## 🧩 3. คำถามเกี่ยวกับโครงสร้างโค้ด

### Q: ใช้ MVC ไหม?

**A:** ไม่ได้ใช้ MVC แบบ framework ทั่วไป แต่ใช้แนวคิดคล้ายกันที่เรียกว่า **Layered Architecture**:

| ชั้น | เทียบกับ MVC | ทำอะไร |
|------|-------------|--------|
| ไฟล์ .php ที่ root + admin/ | Controller + View | รับ request + แสดงผล |
| app/Services/ | Model (Business Logic) | กฎธุรกิจ + ตัดสินใจ |
| app/Repositories/ | Model (Data Access) | คุยกับ database |

ข้อดี: **เข้าใจง่ายกว่า MVC ของ framework** เพราะเห็นทุกขั้นตอนชัดเจน ไม่มี "เวทมนตร์" ซ่อนอยู่

---

### Q: Controller / Service / Repository ต่างกันยังไง?

**A:** เปรียบเทียบกับร้านอาหาร:

- **Controller** = พนักงานรับออเดอร์ → รับคำสั่งจากลูกค้า ตรวจบัตรสมาชิก ส่งต่อให้ครัว
- **Service** = พ่อครัว → ตัดสินใจว่าทำได้ไหม (วัตถุดิบพอไหม? เมนูนี้มีไหม?) แล้วลงมือทำ
- **Repository** = คลังวัตถุดิบ → ไปหยิบของจากตู้เย็น (database) ตามที่พ่อครัวสั่ง

**กฎง่ายๆ:**
- Controller ไม่คิด ไม่ตัดสินใจ แค่ส่งต่อ
- Service คิดและตัดสินใจทุกอย่าง
- Repository แค่ดึง/บันทึกข้อมูล ไม่มีกฎธุรกิจ

---

### Q: ทำไมโค้ดดูแยกหลายไฟล์?

**A:** เพราะ **แยกหน้าที่ให้ชัด แก้ง่าย พังยาก**

ถ้ายัดทุกอย่างไว้ไฟล์เดียว:
- แก้จุดเดียว อาจพังอีก 10 จุด
- อ่านโค้ดไม่รู้ว่าส่วนไหนทำอะไร
- คนหลายคนแก้ไฟล์เดียวกัน ชนกันตลอด

แยกไฟล์แล้ว:
- อยากเปลี่ยนกฎยืมได้กี่เล่ม → แก้แค่ config
- อยากเปลี่ยนหน้าตาเว็บ → แก้แค่ไฟล์ view
- อยากเปลี่ยนวิธีคำนวณค่าปรับ → แก้แค่ BorrowService

---

## 🔐 4. คำถามด้านความปลอดภัย

### Q: ป้องกัน SQL Injection ไหม?

**A:** ป้องกัน ✅

ทุกจุดที่คุยกับ database ใช้ **Prepared Statements** ซึ่งเป็นมาตรฐานป้องกัน SQL Injection

```
❌ ไม่ได้ทำแบบนี้: "SELECT * FROM users WHERE email = '$email'"
✅ ทำแบบนี้: "SELECT * FROM users WHERE email = ?" + bind parameter
```

ตัวแปรที่ผู้ใช้กรอกจะถูกส่งแยกจากคำสั่ง SQL เสมอ คนร้ายจึงแทรกคำสั่ง SQL เข้ามาไม่ได้

---

### Q: เก็บรหัสผ่านยังไง?

**A:** ใช้ **bcrypt** ซึ่งเป็นมาตรฐานสากลสำหรับเข้ารหัสรหัสผ่าน

- รหัสผ่านจะถูก hash ก่อนบันทึกลง database
- แม้คนเจาะ database ได้ ก็ **อ่านรหัสผ่านจริงไม่ออก**
- ระบบไม่เคย decode hash กลับ — ตอนเทียบรหัสผ่าน จะ hash ค่าที่กรอกมาแล้วเทียบกับ hash ที่เก็บ

---

### Q: เอาไปใช้จริงปลอดภัยไหม?

**A:** ระบบมีมาตรการความปลอดภัยพื้นฐานครบ:

| มาตรการ | มีไหม | รายละเอียด |
|---------|-------|-----------|
| ป้องกัน SQL Injection | ✅ | Prepared Statements ทุกจุด |
| เข้ารหัสรหัสผ่าน | ✅ | bcrypt |
| CSRF Protection | ✅ | Token ทุก form |
| Rate Limiting | ✅ | จำกัดการ login ผิดหลายครั้ง |
| Session Security | ✅ | regenerate ID หลัง login |
| Access Control | ✅ | แยก admin/staff/member |
| XSS Prevention | ✅ | escape output ก่อนแสดงผล |

**แต่** ถ้าจะเอาไปใช้กับข้อมูลสำคัญจริงๆ ควรเพิ่ม:
- HTTPS (SSL Certificate)
- Database backup อัตโนมัติ
- ปิด `APP_DEBUG=false`
- ลบ `install.php` หลังติดตั้ง

ดูรายละเอียดเพิ่มเติมได้ที่ `FLOW.md` หัวข้อ "ขอบเขตการใช้งาน"

---

## 🚀 5. การนำไปใช้งานจริง

### Q: เอาไปใช้กับร้านจริงได้ไหม?

**A:** ได้ ถ้าเป็น **ร้านขนาดเล็ก** (สมาชิกไม่เกิน 100 คน, ผู้ใช้พร้อมกันไม่เกิน 5-10 คน)

เช่น:
- ห้องสมุดโรงเรียน
- ร้านเช่าหนังสือ
- ห้องสมุดชุมชน
- ห้องสมุดภายในองค์กรเล็กๆ

---

### Q: ต้องแก้ไขอะไรเพิ่มถ้าจะใช้ production?

**A:** อย่างน้อยควรทำ:

1. **ใช้ HTTPS** — ติดตั้ง SSL Certificate
2. **ตั้ง `APP_DEBUG=false`** — ใน `.env` ไม่งั้นผู้ใช้เห็น error ละเอียดเกินไป
3. **ลบ `install.php`** — ไม่งั้นคนอื่นรัน install ซ้ำได้
4. **ตั้ง backup database** — สำรองข้อมูลเป็นระยะผ่าน phpMyAdmin หรือ cron
5. **เปลี่ยนรหัสผ่าน admin** — อย่าใช้รหัสเริ่มต้น
6. **ตรวจสอบ `.htaccess`** — ให้ทำงานถูกต้องบน hosting จริง

---

### Q: รองรับผู้ใช้เยอะไหม?

**A:** ระบบออกแบบมาสำหรับผู้ใช้ **ไม่มาก**

| ระดับ | รองรับไหม |
|-------|----------|
| 1-10 คนพร้อมกัน | ✅ สบาย |
| 10-30 คนพร้อมกัน | ✅ น่าจะรับได้ (ขึ้นกับ server) |
| 50+ คนพร้อมกัน | ⚠️ อาจช้า |
| 200+ คนพร้อมกัน | ❌ ไม่แนะนำ |

ถ้าต้องการรองรับคนเยอะ ต้องปรับแต่งหลายส่วน เช่น caching, session storage, database optimization ซึ่งอยู่นอกเหนือขอบเขตของ template นี้

---

## 🛠️ 6. การแก้ไขและต่อยอด

### Q: แก้โค้ดได้ไหม?

**A:** ได้เลย! ซื้อแล้วแก้ได้ตามสบาย

แต่แนะนำ:
- **อ่าน `README.md` ก่อน** ว่าไฟล์ไหนแก้ได้ / ไฟล์ไหนห้ามแก้
- **ใช้ Git เก็บเวอร์ชัน** ก่อนแก้อะไร จะได้ย้อนกลับได้ถ้าพัง
- **เริ่มจากสิ่งง่ายๆ** เช่น เปลี่ยนค่าใน `.env` ก่อน แล้วค่อยลงลึก

---

### Q: เพิ่มฟีเจอร์เองได้ไหม?

**A:** ได้ ถ้าเข้าใจโครงสร้าง

| อยากทำ | ต้องทำอะไร |
|--------|-----------|
| เพิ่มหน้าเว็บใหม่ | สร้างไฟล์ .php ใหม่ + `require bootstrap.php` |
| เพิ่มตารางใน DB | เพิ่ม SQL ใน `schema.sql` + สร้าง Repository ใหม่ |
| เพิ่ม business logic | สร้างไฟล์ใหม่ใน `app/Services/` |
| เพิ่มเมนู admin | แก้ `admin/header.php` |
| เพิ่ม API | สร้างไฟล์ใหม่ใน `api/` |

> 💡 **คำแนะนำ:** ถ้าไม่แน่ใจ ให้สร้างไฟล์ใหม่แทนการแก้ไฟล์เดิม จะได้ไม่กระทบส่วนที่ทำงานอยู่แล้ว

---

### Q: ใช้ร่วมกับ frontend / framework อื่นได้ไหม?

**A:** ขึ้นอยู่กับว่าจะใช้แบบไหน:

- **ใช้ CSS framework อื่น (เช่น Bootstrap, Tailwind)** → ได้ แก้ที่ `header.php` + ไฟล์ HTML
- **ใช้ร่วมกับ React / Vue** → ต้องสร้าง REST API เพิ่มเอง เพราะระบบปัจจุบันเป็น server-rendered (PHP สร้าง HTML ส่งให้ browser โดยตรง)
- **ย้ายไป Laravel** → ใช้เป็นแนวทางได้ แต่ต้อง rewrite ใหม่ตามโครงสร้างของ Laravel

---

## 🧾 7. เรื่องการซัพพอร์ต

### Q: ผู้ขายช่วยอะไรบ้าง?

**A:**
- ✅ แนะนำวิธีติดตั้ง + ตั้งค่าเบื้องต้น
- ✅ อธิบายโครงสร้างไฟล์ + flow การทำงาน
- ✅ แก้ bug ที่เกิดจากโค้ดต้นฉบับ (ที่ยังไม่ถูกแก้ไข)
- ✅ ตอบคำถามเกี่ยวกับวิธีตั้งค่า เช่น "เปลี่ยนค่าปรับตรงไหน?"

ดูรายละเอียดเพิ่มเติมที่ `SUPPORT.md`

---

### Q: อะไรที่ไม่อยู่ในขอบเขตซัพพอร์ต?

**A:**
- ❌ เขียนฟีเจอร์ใหม่ให้
- ❌ แก้โค้ดที่ลูกค้าแก้เองแล้วพัง
- ❌ Deploy บน cloud / VPS / Docker
- ❌ สอนเขียนโปรแกรมตั้งแต่เริ่มต้น
- ❌ ปรับแต่งหน้าตาเว็บตามดีไซน์ลูกค้า
- ❌ ซัพพอร์ตไม่มีกำหนดเวลา (มีระยะเวลา 30 วันหลังซื้อ)

---

### Q: ถ้าโค้ดรันไม่ได้ต้องทำยังไง?

**A:** ทำตามขั้นตอนนี้:

1. **เปิด `APP_DEBUG=true`** ในไฟล์ `.env` → จะเห็น error ชัดเจน
2. **อ่าน error message** → ส่วนใหญ่จะบอกว่าพังตรงไหน
3. **เช็คว่าแก้โค้ดเองหรือเปล่า?**
   - ถ้าแก้เอง → ลอง `git checkout` กลับไปเวอร์ชันเดิม
   - ถ้าไม่ได้แก้ → ทักมาหาผู้ขายพร้อม screenshot
4. **ส่งข้อมูลมาให้ครบ:** อธิบายปัญหาสั้นๆ + screenshot + บอกว่าแก้โค้ดอะไรไปบ้าง

> 💡 ข้อความแบบ "ระบบพัง" ตอบยากมาก
> ข้อความแบบ "หลัง install กด login แล้วเจอ error นี้ [รูป] ยังไม่ได้แก้โค้ดอะไร" ตอบง่ายกว่าเยอะ

---

## 💰 8. คำถามเชิงการขาย

### Q: ราคานี้ได้อะไรบ้าง?

**A:** ในราคา 499 บาท คุณได้:

| สิ่งที่ได้ | รายละเอียด |
|-----------|-----------|
| **ซอร์สโค้ดเต็ม** | PHP ทุกไฟล์ ไม่มีซ่อน ไม่มีเข้ารหัส |
| **Database schema** | โครงสร้างตาราง + ข้อมูลตัวอย่าง |
| **ระบบติดตั้งอัตโนมัติ** | `install.php` กดติดตั้งได้เลย |
| **เอกสารประกอบ** | README, FLOW, SUPPORT, FAQ + เอกสารในโฟลเดอร์ docs/ |
| **ซัพพอร์ต 30 วัน** | ถามคำถาม + แจ้ง bug ได้ |

**ไม่รวม:**
- ❌ การ customize ตามความต้องการ
- ❌ การ deploy ให้
- ❌ ซัพพอร์ตตลอดชีพ

---

### Q: มีอัปเดตให้ไหม?

**A:** ถ้ามี **bug สำคัญ** ที่พบในโค้ดต้นฉบับ จะอัปเดตแก้ไขให้

แต่ **ไม่รับประกัน** การอัปเดตฟีเจอร์ใหม่ เพราะเป็น template ที่ขายเป็นชุด ไม่ใช่ SaaS ที่มี subscription

---

### Q: ซื้อแล้วใช้ซ้ำได้ไหม?

**A:** ได้ คุณสามารถ:
- ✅ ใช้กับ project ของตัวเองกี่ project ก็ได้
- ✅ แก้ไข ปรับแต่ง ต่อยอดได้ตามสบาย

---

### Q: ขายต่อ / แจกต่อได้ไหม?

**A:** ❌ **ไม่ได้**

- ห้ามขายซอร์สโค้ดต่อในรูปแบบเดิม
- ห้ามแจกจ่ายซอร์สโค้ดให้คนอื่นฟรี
- ห้าม upload ขึ้น GitHub / GitLab แบบ public

**แต่ได้:**
- ✅ นำไปปรับแต่งเป็น product ใหม่ของคุณเอง แล้วขายเป็น **บริการ** (เช่น "รับทำระบบห้องสมุด") ได้
- ✅ ใช้เป็นฐานในการพัฒนา project ให้ลูกค้าได้

---

## ⚠️ 9. ข้อจำกัดของสินค้า

### Q: ไม่เหมาะกับระบบแบบไหน?

**A:**
- ระบบที่ต้องรองรับ **ผู้ใช้หลักร้อยคนขึ้นไปพร้อมกัน**
- ระบบที่ต้อง **ไม่มีวันล่ม** (High Availability)
- ระบบที่ต้อง **รองรับหลายภาษา** (Internationalization)
- ระบบที่ต้องมี **mobile app** ใช้ร่วมด้วย
- ระบบที่ต้อง **ผ่าน security audit** หรือ compliance เฉพาะทาง
- ระบบที่ต้อง **รันบน Docker / Kubernetes** โดยไม่ปรับแต่ง

---

### Q: ไม่ควรคาดหวังอะไรจาก template นี้?

**A:** พูดตรงๆ ด้วยความเคารพ:

- ❌ **ไม่ใช่ software สำเร็จรูประดับองค์กร** — เป็น template สำหรับเรียนรู้และเริ่มต้น
- ❌ **ไม่มีทุกฟีเจอร์ที่คุณนึกออก** — มีเฉพาะส่วนที่ออกแบบไว้
- ❌ **ไม่ได้ดูสวยเหมือนเว็บระดับบริษัทใหญ่** — เน้น backend มากกว่า design
- ❌ **ไม่ได้ซัพพอร์ตตลอดชีพ** — มีระยะเวลาจำกัด
- ❌ **ไม่ได้เหมาะกับทุกกรณีใช้งาน** — เหมาะกับขนาดเล็กเท่านั้น

**สิ่งที่ควรคาดหวัง:**
- ✅ โค้ดที่ทำงานได้จริง ติดตั้งได้จริง
- ✅ โครงสร้างที่เป็นระเบียบ อ่านเข้าใจได้
- ✅ เอกสารประกอบละเอียด
- ✅ พื้นฐานที่ดีสำหรับต่อยอด

---

## 📌 10. สรุปสำหรับผู้ซื้อ

### Q: ซื้อแล้วควรเริ่มจากอะไร?

**A:** ทำตามนี้เลย:

```
ขั้นที่ 1 │ อ่าน README.md (10 นาที)
          │ → เข้าใจว่าระบบคืออะไร ติดตั้งยังไง
          │
ขั้นที่ 2 │ ติดตั้งระบบ (15 นาที)
          │ → XAMPP + วางโค้ด + .env + install.php
          │
ขั้นที่ 3 │ ทดลองใช้งาน (30 นาที)
          │ → login → เพิ่มหนังสือ → ยืม → คืน → จอง → ดูรายงาน
          │
ขั้นที่ 4 │ อ่าน FLOW.md
          │ → เข้าใจว่าแต่ละ flow ทำงานยังไง
          │
ขั้นที่ 5 │ ลองแก้ค่าใน .env
          │ → เปลี่ยนชื่อระบบ, วันยืม, ค่าปรับ → ดูว่าอะไรเปลี่ยน
          │
ขั้นที่ 6 │ (ถ้าอยากลงลึก) อ่านโค้ดตาม flow
          │ → เริ่มจาก login.php → AuthService → UserRepository
```

---

### Q: ถ้าอยากเก่งขึ้นควรทำยังไงต่อ?

**A:**

1. **อ่านโค้ดตาม flow** — เลือก 1 flow แล้วตามจาก Controller → Service → Repository จนจบ
2. **ลองแก้ไขเล็กๆ** — เช่น เปลี่ยนข้อความ error, เพิ่ม validation rule
3. **ลองเพิ่มฟีเจอร์เล็กๆ** — เช่น เพิ่มหน้าแสดงหนังสือยอดนิยม
4. **เรียน PHP + MySQL เพิ่ม** — มีคอร์สออนไลน์ดีๆ เยอะ ทั้งฟรีและเสียเงิน
5. **เรียนรู้ Git** — จะช่วยเก็บเวอร์ชันโค้ด ย้อนกลับได้ถ้าพัง
6. **ลองเรียน Laravel** — เมื่อเข้าใจ PHP ล้วนแล้ว จะเข้าใจ framework ได้เร็วมาก

> 🎯 Template นี้เป็น **จุดเริ่มต้นที่ดี** ไม่ใช่จุดจบ ยิ่งอ่านยิ่งแก้ ยิ่งเข้าใจ backend มากขึ้น

---

*เอกสารนี้เป็นส่วนหนึ่งของชุดโค้ด "ระบบยืมคืนหนังสือ" — พร้อมใส่ในแพ็กขายได้ทันที*
