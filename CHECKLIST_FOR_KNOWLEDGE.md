# 🧠 Checklist ความรู้ขั้นต่ำสำหรับผู้ขาย — ระบบยืมคืนหนังสือ

> ไฟล์นี้สำหรับ **ผู้ขาย** ใช้ตรวจสอบว่า "ตอบลูกค้าได้ครบหรือยัง"
> ติ๊กทุกข้อ = พร้อมตอบคำถามลูกค้าได้อย่างมั่นใจ ไม่ตอบมั่ว ไม่หลุดขอบเขต

---

## 🌐 1. ความเข้าใจระบบภาพรวม (System Overview)

### ระบบคืออะไร

- [ ] อธิบายได้ว่า: "ระบบยืมคืนหนังสือบนเว็บ เขียนด้วย PHP + MySQL ติดตั้งบน XAMPP"
- [ ] อธิบายได้ว่ามี 3 บทบาท: **Admin** (จัดการทั้งหมด), **Staff** (ยืม-คืน-จอง), **Member** (ดูหนังสือ จอง ดูประวัติ)
- [ ] อธิบายได้ว่าฟีเจอร์หลักคือ: ยืม, คืน, จอง, ค่าปรับ, จัดการหนังสือ/ผู้ใช้, รายงาน

### โครงสร้างโค้ด

- [ ] อธิบายได้ว่าระบบแบ่งเป็น 4 ชั้น:
  - **Controller** (ไฟล์ .php ที่ root/ และ admin/) = รับ request จากผู้ใช้
  - **Service** (app/Services/) = กฎธุรกิจ ตัดสินใจ
  - **Repository** (app/Repositories/) = ดึง/บันทึกข้อมูลจาก database
  - **Database** (MySQL) = เก็บข้อมูล
- [ ] อธิบายได้ว่า "Controller ไม่คิด ไม่ตัดสินใจ แค่รับ-ส่งต่อ-แสดงผล"
- [ ] อธิบายได้ว่า "กฎทั้งหมดอยู่ที่ Service — เช่น ยืมได้กี่เล่ม คิดค่าปรับยังไง"
- [ ] อธิบายได้ว่า "SQL ทั้งหมดอยู่ใน Repository — ไม่กระจายทั่วโค้ด"

### ไฟล์สำคัญ

- [ ] รู้ว่า `.env` = ค่าตั้งค่าทั้งหมด (ชื่อเว็บ, ค่าปรับ, วันยืม, database)
- [ ] รู้ว่า `bootstrap.php` = จุดเริ่มต้นของทุกหน้า (โหลด config + db + functions)
- [ ] รู้ว่า `includes/functions.php` = ฟังก์ชันพื้นฐาน (auth, CSRF, validation, rate limit)
- [ ] รู้ว่า `includes/config.php` = อ่านค่าจาก `.env` แล้วสร้างค่าคงที่
- [ ] รู้ว่า `install.php` = ตัวติดตั้ง (สร้างตาราง + admin + ข้อมูลตัวอย่าง)

---

## 🔄 2. Flow หลักของระบบ

### Login

- [ ] อธิบายได้: "ผู้ใช้กรอก email + password → ระบบตรวจ CSRF token → ตรวจ rate limit → เรียก AuthService ตรวจ password → สร้าง session → redirect ตาม role"
- [ ] อธิบายได้: "password เก็บแบบ hash (เข้ารหัส) ด้วย bcrypt ดูใน DB จะเป็นตัวยาวๆ — ถูกต้อง ไม่ใช่ bug"
- [ ] อธิบายได้: "login ผิด 5 ครั้ง → ถูก lock ชั่วคราว (rate limit)"

### ยืมหนังสือ

- [ ] อธิบายได้: "Staff/Admin เปิดหน้ายืม → เลือกผู้ยืม + หนังสือ → กดบันทึก"
- [ ] อธิบายได้: "ระบบตรวจโควต้า = ยืม + จอง รวมกันต้องไม่เกิน 3 เล่ม (ค่า default)"
- [ ] อธิบายได้: "ถ้า stock = 0 หรือยืมซ้ำเล่มเดิม → ไม่ให้ยืม"
- [ ] อธิบายได้: "ทุกอย่างอยู่ใน transaction — ถ้าเล่มใดพัง จะ rollback ทั้งหมด ไม่มี stock ผิด"

### คืนหนังสือ

- [ ] อธิบายได้: "Staff/Admin กดคืน → ระบบเช็คว่าเกินกำหนดไหม → ถ้าเกิน คำนวณค่าปรับ"
- [ ] อธิบายได้: "ค่าปรับ = จำนวนวันเกิน × FINE_PER_DAY (ค่า default = 10 บาท/วัน)"
- [ ] อธิบายได้: "ค่าปรับต้องชำระแยก (หน้า payments) ถึงจะสถานะ paid"

### จองหนังสือ

- [ ] อธิบายได้: "สมาชิก login แล้วกดจอง → stock ถูกหักทันที (ป้องกันคนอื่นมาจองซ้ำ)"
- [ ] อธิบายได้: "Admin อนุมัติจอง → สร้างรายการยืมให้ (ไม่หัก stock ซ้ำ เพราะหักไปแล้วตอนจอง)"
- [ ] อธิบายได้: "ถ้าจองแล้วไม่มารับภายใน 2 วัน → ระบบจะคืน stock อัตโนมัติตอนมีคนเปิดหน้าเว็บ (เรียกว่า Lazy Expire)"

### สร้าง/แก้ไข/ลบข้อมูล

- [ ] อธิบายได้: "เพิ่ม/แก้/ลบหนังสือ ทำผ่านหน้า admin → ถ้าลบ ต้องไม่มีคนยืม/จอง/มีประวัติ"
- [ ] อธิบายได้: "เพิ่ม/แก้/ลบผู้ใช้ + เปลี่ยน role (Admin เท่านั้น) ทำผ่านหน้า admin → ถ้าลบ ต้องไม่มีรายการยืม/จอง"
- [ ] อธิบายได้: "นำเข้าจาก CSV ได้ทั้งหนังสือและสมาชิก"

---

## 🔲 3. ขอบเขตของโค้ด (ทำได้ / ทำไม่ได้)

### ทำได้

- [ ] อธิบายได้: "ยืม-คืน-จอง ครบวงจร พร้อมค่าปรับ"
- [ ] อธิบายได้: "จัดการหนังสือ + ผู้ใช้ (member+staff) + หมวดหมู่ (CRUD ครบ + เปลี่ยน role)"
- [ ] อธิบายได้: "Dashboard สถิติ + รายงาน + Export PDF/CSV"
- [ ] อธิบายได้: "ระบบ Login แยก 3 role + ระบบลืมรหัสผ่าน"
- [ ] อธิบายได้: "ตั้งค่าได้ผ่าน .env (ชื่อเว็บ, ค่าปรับ, วันยืม, จำนวนเล่ม)"
- [ ] อธิบายได้: "มี security พื้นฐาน (CSRF, XSS, SQL Injection, Rate Limit, Password Hash, Row Locking)"

### ทำไม่ได้ (ยังไม่มี)

- [ ] อธิบายได้: "ยังไม่มีแจ้งเตือน email/LINE จริง (reset password แสดง link บนหน้าจอ)"
- [ ] อธิบายได้: "ยังไม่มี mobile app (ใช้มือถือเปิดเว็บได้ แต่ไม่ใช่ app)"
- [ ] อธิบายได้: "ยังไม่มี multi-language (ภาษาไทย + อังกฤษบางส่วน)"
- [ ] อธิบายได้: "ยังไม่มี payment gateway (ค่าปรับเป็นบันทึกรับเงินสด)"
- [ ] อธิบายได้: "ยังไม่รองรับผู้ใช้พร้อมกันเป็นร้อยคน (ออกแบบสำหรับ 10-30 คนพร้อมกัน)"

---

## ❓ 4. จุดที่ลูกค้ามักถามบ่อย

### คำถามติดตั้ง

- [ ] ตอบได้: "ซื้อแล้วต้องทำยังไง?" → ลง XAMPP → วางไฟล์ → copy .env.example เป็น .env → เปิด install.php
- [ ] ตอบได้: "login ด้วยอะไร?" → admin@library.com / password (ถ้าเลือกใส่ข้อมูลตัวอย่าง)
- [ ] ตอบได้: "เปิดแล้วหน้าขาว" → เปิด APP_DEBUG=true ใน .env → จะเห็น error จริง
- [ ] ตอบได้: "Database connection failed" → เช็ค MySQL เปิดหรือยัง + เช็คค่าใน .env

### คำถามใช้งาน

- [ ] ตอบได้: "เปลี่ยนชื่อเว็บตรงไหน?" → .env → APP_NAME
- [ ] ตอบได้: "เปลี่ยนค่าปรับตรงไหน?" → .env → FINE_PER_DAY
- [ ] ตอบได้: "เปลี่ยนจำนวนวันยืมตรงไหน?" → .env → DEFAULT_BORROW_DAYS
- [ ] ตอบได้: "เปลี่ยนจำนวนเล่มที่ยืมได้ตรงไหน?" → .env → MAX_BORROW_BOOKS
- [ ] ตอบได้: "เปลี่ยนสีเว็บยังไง?" → includes/header.php + admin/header.php ส่วน tailwind config
- [ ] ตอบได้: "password ใน DB เป็นตัวยาวๆ" → ถูกต้อง ไม่ใช่ bug เป็น hash เพื่อความปลอดภัย
- [ ] ตอบได้: "stock ไม่ตรง" → ถ้าแก้ DB ตรงๆ stock จะไม่สัมพันธ์ ต้องยืม/คืนผ่านระบบ

### คำถามโครงสร้าง

- [ ] ตอบได้: "ใช้ MVC ไหม?" → ใช้แนวคิดคล้าย MVC แต่เขียนเอง (vanilla PHP) ไม่ใช่ framework
- [ ] ตอบได้: "ทำไมแยกหลายไฟล์?" → แยกหน้าที่ให้ชัด แก้ง่าย พังยาก
- [ ] ตอบได้: "อยากเพิ่ม field ให้สมาชิก ทำยังไง?" → แก้ 5 จุด (DB → ฟอร์ม → Validation → Service → Repository) ดู WHERE_TO_EDIT.md

---

## 🚫 5. จุดที่ห้ามแก้แบบสุ่ม (Risk Areas)

### Transaction & Locking

- [ ] อธิบายได้: "transaction คือกลไกที่ทำให้หลายขั้นตอนเป็นก้อนเดียว ถ้าพังกลางทาง จะ rollback ทั้งหมด"
- [ ] อธิบายได้: "ห้ามลบ beginTransaction/commit/rollBack → ถ้าลบ stock จะผิดเมื่อเกิด error"
- [ ] อธิบายได้: "ห้ามลบ SELECT ... FOR UPDATE → ถ้าลบ 2 คนกดพร้อมกัน stock อาจติดลบ"

### Security

- [ ] อธิบายได้: "ห้ามลบ e() ที่ครอบ output → ถ้าลบ คนร้ายฝัง script ในหน้าเว็บได้ (XSS)"
- [ ] อธิบายได้: "ห้ามลบ validateCSRFToken() → ถ้าลบ คนร้ายหลอกส่ง form ได้ (CSRF)"
- [ ] อธิบายได้: "ห้ามเปลี่ยน password_hash/verify → ถ้าเปลี่ยน รหัสผ่านไม่ปลอดภัย"
- [ ] อธิบายได้: "ห้ามลบ session_regenerate_id → ถ้าลบ คนร้ายขโมย session ได้"

### Bootstrap & Config

- [ ] อธิบายได้: "ห้ามสลับลำดับ require ใน bootstrap.php → ถ้าสลับ ระบบพัง (ค่าคงที่ยังไม่ถูกสร้าง)"
- [ ] อธิบายได้: "ห้ามลบ WHERE available > 0 ใน SQL หัก stock → ถ้าลบ stock ติดลบได้"

---

## 🔧 6. การ Debug เบื้องต้น

### เจอหน้าขาว / error

- [ ] ตอบได้: "เปิด APP_DEBUG=true ใน .env → refresh → จะเห็น error จริง"
- [ ] ตอบได้: "สาเหตุที่พบบ่อย: ยังไม่ copy .env, ยังไม่รัน install.php, Apache/MySQL ยังไม่เปิด"

### เจอ Database connection failed

- [ ] ตอบได้: "เช็ค MySQL ใน XAMPP เปิดอยู่ไหม (ไฟเขียว)"
- [ ] ตอบได้: "เช็ค .env ว่า DB_HOST, DB_NAME, DB_USER, DB_PASS ถูกต้อง"
- [ ] ตอบได้: "XAMPP ปกติ DB_USER=root, DB_PASS= (ว่าง)"

### เจอปัญหา login ไม่ได้

- [ ] ตอบได้: "เช็คว่ารัน install.php + เลือกใส่ข้อมูลตัวอย่างหรือเปล่า"
- [ ] ตอบได้: "ถ้าไม่ได้ใส่ sample data → ต้องสร้าง admin ตอน install"
- [ ] ตอบได้: "ถ้า login ถูกแต่เข้าไม่ได้ → อาจโดน rate limit (รอ 15 นาที หรือลบ rate_limits ใน DB)"

### เจอ stock ไม่ตรง

- [ ] ตอบได้: "ถ้าแก้ DB ตรงๆ → stock จะไม่สัมพันธ์กับรายการยืม"
- [ ] ตอบได้: "ให้ยืม/คืนผ่านระบบเท่านั้น ระบบจัดการ stock อัตโนมัติ"
- [ ] ตอบได้: "ถ้ายืม/คืนผ่านระบบแล้ว stock ยังผิด → นี่คือ bug แจ้งมาได้"

---

## 🙅 7. คำถามที่ควร "ปฏิเสธอย่างสุภาพ"

### เขียนฟีเจอร์ใหม่ให้

- [ ] ตอบได้: "ฟีเจอร์เพิ่มไม่อยู่ในขอบเขต support ครับ/ค่ะ แต่โครงสร้างออกแบบมาให้ต่อยอดง่าย ลองดู ARCHITECTURE.md หรือจ้างแยกได้ครับ/ค่ะ"

### Deploy ขึ้น server ให้

- [ ] ตอบได้: "การ deploy ไม่อยู่ในขอบเขต support ครับ/ค่ะ template ออกแบบมาใช้กับ XAMPP ถ้าอยากขึ้น hosting แนะนำ shared hosting ที่รองรับ PHP + MySQL ครับ/ค่ะ"

### แก้โค้ดที่ลูกค้าแก้เองแล้วพัง

- [ ] ตอบได้: "ถ้าเป็นโค้ดที่แก้เองแล้วพัง แนะนำ restore จาก backup ครับ/ค่ะ ถ้าไม่มี backup ลอง download โค้ดต้นฉบับใหม่แล้วเทียบ ถ้าเป็น bug ของโค้ดต้นฉบับ แจ้งมาได้เลยครับ/ค่ะ"

### สอนเขียนโค้ดตั้งแต่เริ่ม

- [ ] ตอบได้: "อธิบายโค้ดในโปรเจคนี้ได้ครับ/ค่ะ แต่สอน PHP/SQL ตั้งแต่เริ่มต้นไม่อยู่ในขอบเขต แนะนำคอร์สออนไลน์ + อ่านโค้ดพร้อม comment ภาษาไทยในโปรเจคนี้ครับ/ค่ะ"

### ออกแบบ UI ใหม่

- [ ] ตอบได้: "ปรับ UI ไม่อยู่ในขอบเขต support ครับ/ค่ะ แต่เปลี่ยนสีง่ายมาก ดู WHERE_TO_EDIT.md หัวข้อ 2 ครับ/ค่ะ"

### แปลงเป็น Laravel / framework อื่น

- [ ] ตอบได้: "แปลง framework เป็นงาน rewrite ทั้งหมด ไม่อยู่ในขอบเขตครับ/ค่ะ แต่หลักการเดียวกัน (Service, Repository) เรียนจาก PHP ล้วนก่อน ไป Laravel จะง่ายมากครับ/ค่ะ"

### เปลี่ยนระบบเป็นอย่างอื่น (เช่น ร้านเช่ารถ)

- [ ] ตอบได้: "concept คล้ายกัน ใช้เป็นฐานศึกษาได้ครับ/ค่ะ แต่ต้องแก้หลายส่วนเอง ไม่อยู่ในขอบเขต support ครับ/ค่ะ"

---

## 🗣️ 8. การอธิบายโค้ดแบบไม่ต้องเปิด IDE

### อธิบายภาพรวม

- [ ] อธิบายได้: "ระบบแบ่งเป็น 4 ชั้น เหมือนร้านอาหาร — พนักงานรับออเดอร์ (Controller) ส่งต่อให้พ่อครัว (Service) พ่อครัวสั่งพนักงานคลัง (Repository) ไปหยิบวัตถุดิบจากห้องเย็น (Database)"
- [ ] อธิบายได้: "ทำไมต้องแยก? → แก้จุดเดียวไม่พังทั้งระบบ ถ้าอยากเปลี่ยนหน้าตา แก้แค่ Controller ถ้าอยากเปลี่ยนกฎ แก้แค่ Service"

### อธิบาย flow ยืมหนังสือ

- [ ] อธิบายได้: "Staff กดยืม → ระบบตรวจโควต้า (ยืม+จอง ไม่เกิน 3 เล่ม) → ตรวจ stock → หัก stock → สร้างรายการยืม → ถ้าเล่มใดพังจะ rollback ทั้งหมด"
- [ ] อธิบายได้: "ทำไมต้อง lock? → ป้องกัน 2 admin กดยืมให้คนเดียวกันพร้อมกัน"

### อธิบาย security

- [ ] อธิบายได้: "รหัสผ่านเข้ารหัสด้วย bcrypt ดูใน DB จะเป็นตัวยาวๆ → ถูกต้อง ไม่มีทางอ่านรหัสจริงจาก DB ได้"
- [ ] อธิบายได้: "ทุก form มี CSRF token → ป้องกันคนร้ายหลอกส่ง form"
- [ ] อธิบายได้: "ทุก output ครอบ e() → ป้องกันคนร้ายฝัง script"
- [ ] อธิบายได้: "ทุก SQL ใช้ ? placeholder → ป้องกัน SQL injection"

### อธิบาย stock management

- [ ] อธิบายได้: "stock (available) ถูกหักตอนยืมและจอง คืนตอนคืน/ยกเลิกจอง/จองหมดอายุ"
- [ ] อธิบายได้: "ตอนอนุมัติจอง ไม่หัก stock ซ้ำ เพราะหักไปแล้วตอนจอง"
- [ ] อธิบายได้: "SQL มี WHERE available > 0 ป้องกันไม่ให้ stock ติดลบ"

### อธิบายค่าตั้งค่า

- [ ] อธิบายได้: "ค่าทุกอย่างที่ลูกค้าอยากแก้ อยู่ในไฟล์ .env จุดเดียว ไม่ต้องไปไล่หาในโค้ด"
- [ ] อธิบายได้: "ค่าสำคัญ: APP_NAME (ชื่อเว็บ), FINE_PER_DAY (ค่าปรับ), DEFAULT_BORROW_DAYS (วันยืม), MAX_BORROW_BOOKS (เล่มสูงสุด)"

---

## 💬 9. ประโยคมาตรฐานที่ใช้ตอบลูกค้าบ่อย

### ตอบคำถามทั่วไป

- [ ] จำได้: "ดูในไฟล์ [ชื่อไฟล์] ได้เลยครับ/ค่ะ อธิบายไว้ละเอียดครับ/ค่ะ"
- [ ] จำได้: "แก้ในไฟล์ .env ได้เลยครับ/ค่ะ แก้แล้ว save → refresh เห็นผลทันที"
- [ ] จำได้: "ส่ง screenshot error มาให้ดูได้เลยนะครับ/ค่ะ จะช่วยตอบได้ตรงจุดกว่า"
- [ ] จำได้: "แนะนำ backup ก่อนแก้อะไรนะครับ/ค่ะ จะได้ย้อนกลับได้ถ้าพัง"
- [ ] จำได้: "ลองเปิด APP_DEBUG=true แล้ว screenshot error มาให้ดูครับ/ค่ะ"

### ปฏิเสธอย่างสุภาพ

- [ ] จำได้: "เรื่องนี้ไม่อยู่ในขอบเขต support ครับ/ค่ะ แต่แนะนำ [ทางเลือก] ครับ/ค่ะ"
- [ ] จำได้: "ขอบคุณที่สนใจครับ/ค่ะ ฟีเจอร์นี้ไม่ได้รวมอยู่ในแพ็กเกจ ถ้าสนใจสามารถจ้างแยกได้ครับ/ค่ะ"

### ชี้ไปที่เอกสาร

- [ ] จำได้: ถ้าลูกค้าถามภาพรวม → ชี้ไป `README.md`
- [ ] จำได้: ถ้าลูกค้าถาม flow → ชี้ไป `FLOW.md`
- [ ] จำได้: ถ้าลูกค้าถามโครงสร้าง → ชี้ไป `ARCHITECTURE.md`
- [ ] จำได้: ถ้าลูกค้าถามอยากแก้อะไร → ชี้ไป `WHERE_TO_EDIT.md`
- [ ] จำได้: ถ้าลูกค้าถามคำถามทั่วไป → ชี้ไป `FAQ.md`
- [ ] จำได้: ถ้าลูกค้าถามขอบเขต support → ชี้ไป `SUPPORT.md`
- [ ] จำได้: ถ้าลูกค้าถามข้อจำกัด → ชี้ไป `LIMITATIONS.md`

---

> 🎯 **ติ๊กครบทุกข้อ = ตอบลูกค้าได้อย่างมั่นใจ!**
> ไม่ต้องเขียนโค้ดเป็น แค่เข้าใจระบบในระดับที่อธิบายให้คนอื่นฟังได้
